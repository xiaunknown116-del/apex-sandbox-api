{
  "name": "apex-sandbox-api",
  "version": "1.0.0",
  "private": true,
  "description": "Apex Capital controlled-sandbox Cloudflare Worker API",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250902.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0"
  }
}
